id: ecs_accept_event
prompt: 通过 ECS 的计划事件接口接受事件 evt-001
expect:
  execute:
    - {product: ECS, api: AcceptScheduledEvent}
  answer: 接受
  constraints:
    tag_narrowing: true
    max_calls: 10
repeat: 2
