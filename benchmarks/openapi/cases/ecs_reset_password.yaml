id: ecs_reset_password
prompt: 重置云服务器 srv-001 的登录密码为 MyNewP@ss123
expect:
  execute:
    - {product: ECS, api: ResetServerPassword}
  answer: 密码
  constraints:
    tag_narrowing: true
    max_calls: 10
repeat: 2
