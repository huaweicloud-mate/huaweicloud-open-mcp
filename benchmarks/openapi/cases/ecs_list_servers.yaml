id: ecs_list_servers
prompt: 帮我查询北京四地域的云服务器列表
expect:
  execute:
    - {product: ECS, api: ListServersDetails}
    - {product: ECS, api: ListCloudServers}
  answer: 服务器
  constraints:
    tag_narrowing: true
    max_calls: 8
fixture:
  apis:
    ECS/ListServersDetails:
      body:
        count: 1
        servers:
          - {id: stub-srv-1, name: bench-server, status: ACTIVE, flavor: s6.small.1}
    ECS/ListCloudServers:
      body:
        count: 1
        servers:
          - {id: stub-srv-1, name: bench-server, status: ACTIVE, flavor: s6.small.1}
labels:
  capability: retrieval
  difficulty: easy