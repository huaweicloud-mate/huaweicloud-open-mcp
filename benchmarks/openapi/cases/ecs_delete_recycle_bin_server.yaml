id: ecs_delete_recycle_bin_server
prompt: 从回收站中删除虚拟机 srv-001
expect:
  execute:
    - {product: ECS, api: DeleteRecycleBinServer}
  answer: 删除
  constraints:
    tag_narrowing: true
    max_calls: 10
repeat: 2
