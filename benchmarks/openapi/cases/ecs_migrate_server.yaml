id: ecs_migrate_server
prompt: 冷迁移云服务器 srv-001
expect:
  execute:
    - {product: ECS, api: MigrateServer}
  answer: 迁移
  constraints:
    tag_narrowing: true
    max_calls: 10
fixture:
  apis:
    ECS/MigrateServer:
      body: {job_id: stub-job-migrate}
labels:
  capability: write-execution
  difficulty: medium
repeat: 2
