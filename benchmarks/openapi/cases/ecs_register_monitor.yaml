id: ecs_register_monitor
prompt: 在 ECS 的状态管理中为云服务器 srv-001 注册监控
expect:
  execute:
    - {product: ECS, api: RegisterServerMonitor}
  answer: 监控
  constraints:
    tag_narrowing: true
    max_calls: 10
repeat: 2
