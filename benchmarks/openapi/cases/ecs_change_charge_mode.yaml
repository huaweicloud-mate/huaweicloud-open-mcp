id: ecs_change_charge_mode
prompt: 将云服务器 srv-001 的计费模式更改为包年包月
expect:
  execute:
    - {product: ECS, api: ChangeServerChargeMode}
  answer: 计费
  constraints:
    tag_narrowing: true
    max_calls: 10
repeat: 2
